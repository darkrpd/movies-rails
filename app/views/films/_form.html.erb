<%= form_for(@film) do |f| %>
  <% if @film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@film.errors.count, "error") %> prohibited this film from being saved:</h2>

      <ul>
      <% @film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :details %><br>
    <%= f.text_field :details %>
  </div>
  <h1>Choose cast</h1>

  <div class="field">
    <% @casts.each do |cast| %>
      <input type="checkbox" name="cast_ids[]" value="<%= cast.id %>"/>
      <%= f.label cast.name %>
    <% end %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
